# Local dev (SQLite). For production use a Postgres URL and switch the
# provider in prisma/schema.prisma to "postgresql".
DATABASE_URL="file:./dev.db"

# Generate a strong one: openssl rand -base64 32
SESSION_SECRET="change-this-secret-in-production-please-32chars-minimum"
